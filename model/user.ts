export interface UserProp {
    name:string,
    collection:object,
    Fortnight:object,
    listeningTo: string,
    focusingOn: string,
    focusFor:number,
    chillFor:number,
    theme:string,
    background:string,
}